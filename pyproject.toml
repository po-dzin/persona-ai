[project]
name = "live-photo-vertical-slice"
version = "0.1.0"
description = "Vertical slice MVP scaffold for Live Photo App"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
pythonpath = [".", "apps/api"]

[tool.ruff]
line-length = 100
